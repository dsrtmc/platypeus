/**
 * Specifies how many words we want to load at once during the tests. Generally as many as possible is the best,
 * granted it doesn't noticeably affect the performance.
 */
export const LOADED_WORDS_COUNT = 30;
