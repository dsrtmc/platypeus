query GetUserByUsername($username: String!) {
    userByUsername(username: $username) {
        username
        scores {
            id
            averageWpm
        }
        createdAt
        updatedAt
    }
}