query GetScores($after: String, $before: String, $first: Int, $last: Int, $order: [ScoreSortInput!]) {
    scores(after: $after, before: $before, first: $first, last: $last, order: $order) {
        edges {
            node {
                id
                averageWpm
                user {
                    username
                }
            }
            cursor
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}