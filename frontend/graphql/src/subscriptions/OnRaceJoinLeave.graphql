subscription OnRaceJoinLeave($raceId: UUID!) {
    onRaceJoinLeave(raceId: $raceId) {
        mode
        modeSetting
        racers {
            id
            username
        }
        host {
            id
            username
        }
        running
        finished
        content
        chatboxId
    }
}
