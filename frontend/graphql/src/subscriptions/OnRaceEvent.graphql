subscription OnRaceEvent($raceId: UUID!) {
    onRaceEvent(raceId: $raceId) {
        id
        mode
        modeSetting
        racers {
            id
            username
        }
        host {
            id
            username
        }
        running
        finished
        content
        chatboxId
    }
}
