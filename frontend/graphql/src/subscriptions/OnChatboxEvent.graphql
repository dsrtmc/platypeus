subscription OnChatboxEvent($chatboxId: UUID!) {
    onChatboxEvent(chatboxId: $chatboxId) {
        messages {
            id
            author {
                username
            }
            content
            createdAt
        }
    }
}
