mutation CreateRace($input: CreateRaceInput!) {
    createRace(input: $input) {
        race {
            id
            racers {
                user {
                    username
                }
            }
            private
        }
    }
}