mutation FinishRace($input: FinishRaceInput!) {
    finishRace(input: $input) {
        race {
            id
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}