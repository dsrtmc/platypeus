mutation RunRace($input: RunRaceInput!) {
    runRace(input: $input) {
        race {
            running
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}