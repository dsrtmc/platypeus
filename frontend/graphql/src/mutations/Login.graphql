mutation Login($input: LoginInput!) {
    login(input: $input) {
        user {
            id
            ...UserInfoFragment
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}