mutation Login($input: LoginInput!) {
    login(input: $input) {
        user {
            id
            username
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}