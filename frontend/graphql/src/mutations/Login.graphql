mutation Login($input: LoginInput!) {
    login(input: $input) {
        user {
            ...MeFragment
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}