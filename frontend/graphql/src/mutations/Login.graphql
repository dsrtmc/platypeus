mutation Login($input: LoginInput!) {
    login(input: $input) {
        user {
            id
            username
        }
        errors {
            __typename
            ... on Error {
                message
            }
        }
    }
}