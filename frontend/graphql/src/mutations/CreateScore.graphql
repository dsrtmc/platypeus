mutation CreateScore($input: CreateScoreInput!) {
    createScore(input: $input) {
        score {
            id
            wpm
            rawWpm
            mode
            modeSetting
            language
            user {
                username
            }
        }
    }
}