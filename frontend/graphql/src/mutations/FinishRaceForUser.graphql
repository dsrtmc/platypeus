mutation FinishRaceForUser($input: FinishRaceForUserInput!) {
    finishRaceForUser(input: $input) {
        racer {
            id
            finished
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}