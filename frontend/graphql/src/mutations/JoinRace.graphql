mutation JoinRace($input: JoinRaceInput!) {
    joinRace(input: $input) {
        race {
            racers {
                user {
                    username
                }
                wpm
            }
        }
    }
}