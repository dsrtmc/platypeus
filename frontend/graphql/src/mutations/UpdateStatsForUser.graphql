mutation UpdateStatsForUser($input: UpdateStatsForUserInput!) {
    updateStatsForUser(input: $input) {
        race {
            racers {
                user {
                    username
                }
                wpm
            }
        }
        errors {
            code: __typename
            ... on Error {
                message
            }
        }
    }
}
